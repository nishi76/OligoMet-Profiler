% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blank_correction.R
\name{apply_blank_correction}
\alias{apply_blank_correction}
\title{Add blank-corrected signal columns to \code{ms1_matches}}
\usage{
apply_blank_correction(batch_matches, sample_meta, signal_col = NULL)
}
\arguments{
\item{batch_matches}{The \code{ms1_matches} data.frame from
\code{\link[=match_ms1_batch]{match_ms1_batch()}}/\code{\link[=annotate_metabolites_batch]{annotate_metabolites_batch()}}.}

\item{sample_meta}{A data.frame with \code{sample} and \code{sample_type} columns.}

\item{signal_col}{Which column(s) to correct. \code{NULL} (the default)
corrects every one of \code{intensity}/\code{area} present in \code{batch_matches}.
A single column name corrects only that one.}
}
\value{
\code{batch_matches} with \code{intensity_bcorr}/\code{area_bcorr} columns
added when blanks were found; unchanged otherwise.
}
\description{
Computes the pooled per-metabolite blank value (see
\code{\link[=compute_blank_signal]{compute_blank_signal()}}) for \code{intensity} and/or \code{area} (whichever are
present in \code{batch_matches}) and adds \code{intensity_bcorr}/\code{area_bcorr}
columns: \code{max(0, signal - blank_mean)} for that metabolite, floored at
zero. If no reagent/matrix blank samples are found at all, the input
is returned unchanged (no \verb{_bcorr} columns added), so callers can check
\code{"area_bcorr" \%in\% names(m)} to know whether blank correction is
available.
}
\seealso{
\code{\link[=compute_blank_signal]{compute_blank_signal()}}
}
