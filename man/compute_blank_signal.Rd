% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blank_correction.R
\name{compute_blank_signal}
\alias{compute_blank_signal}
\title{Compute the pooled per-metabolite blank signal}
\usage{
compute_blank_signal(batch_matches, sample_meta, signal_col = NULL)
}
\arguments{
\item{batch_matches}{The \code{ms1_matches} data.frame from
\code{\link[=match_ms1_batch]{match_ms1_batch()}}/\code{\link[=annotate_metabolites_batch]{annotate_metabolites_batch()}}.}

\item{sample_meta}{A data.frame with \code{sample} and \code{sample_type}
columns (used together with the sample name itself to identify
blanks -- see \code{\link[=.is_blank_sample]{.is_blank_sample()}}).}

\item{signal_col}{Which column to summarize. \code{NULL} (the default)
auto-detects: \code{"area"} if present and not all-\code{NA}, else
\code{"intensity"}.}
}
\value{
A list: \code{table} (a data.frame with \code{met_id}, \code{met_name},
\code{blank_mean}, \code{blank_sd}, \code{n_blank} -- one row per metabolite present
in \code{batch_matches}), and \code{note} (empty string on success, else why no
blank samples were found).
}
\description{
Averages a metabolite's raw signal across every row whose sample is a
reagent or matrix blank (see \code{\link[=.is_blank_sample]{.is_blank_sample()}}), pooling both blank
types into a single value per metabolite. A metabolite with zero blank
observations gets \code{blank_mean = 0} (so \code{\link[=apply_blank_correction]{apply_blank_correction()}}
leaves it unchanged), rather than being dropped.
}
\seealso{
\code{\link[=apply_blank_correction]{apply_blank_correction()}}
}
