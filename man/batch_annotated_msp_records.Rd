% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mirror_plot.R
\name{batch_annotated_msp_records}
\alias{batch_annotated_msp_records}
\title{Export every confirmed batch MS2 hit's acquired spectrum as MSP records}
\usage{
batch_annotated_msp_records(
  mets,
  dict,
  ms2_confirmations,
  ms2_spectra,
  tol_ppm = 25,
  z_range = 1:2,
  h_offset = 0
)
}
\arguments{
\item{mets}{A list of metabolite objects (see [generate_metabolites()]).}

\item{dict}{A chemistry dictionary (see [build_dictionary()]).}

\item{ms2_confirmations}{Output of [confirm_ms2_batch()]/
[annotate_metabolites_batch()].}

\item{ms2_spectra}{The `"spectra"` attribute from the same call.}

\item{tol_ppm, z_range, h_offset}{Fragment matching parameters -- see
[confirm_metabolite()]/[match_ms1()].}
}
\value{
A list of spectrum records (see `.spectrum_record()`/
  [write_msp()]), one per confirmed hit.
}
\description{
Exports the ACQUIRED spectrum (real instrument data) for every
confirmed batch hit, with each peak annotated by the fragment ion the
app's own in-memory confirmation already matched it to (blank where
unmatched). More precise than re-matching an exported MSP library file
by precursor-mz/charge lookup, since it reuses the exact per-hit
confirmation already computed for that (sample, metabolite, charge,
adduct) rather than the nearest library candidate.
}
\seealso{
[batch_mirror_plots_pdf()], [write_msp()]
}
